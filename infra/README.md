# Infrastructure

The repository-level `compose.yaml` defines the local MVP stack:

- PostgreSQL 16
- Redis 7
- FastAPI backend
- Next.js frontend

Production deployment configuration is intentionally deferred to the deployment milestone.
